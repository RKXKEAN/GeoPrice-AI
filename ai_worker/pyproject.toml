[project]
name = "geoprice-ai-worker"
version = "0.1.0"
description = "ARQ Worker for YOLO and LSTM Processing"
requires-python = ">=3.12"
dependencies = [
    "arq",
    "redis",
    "torch",
    "ultralytics",
    "mlflow",
    "boto3",
    "httpx"
]